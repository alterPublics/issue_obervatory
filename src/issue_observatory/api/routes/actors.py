"""Actor listing, detail, and entity resolution routes."""

from __future__ import annotations

from fastapi import APIRouter

router = APIRouter()
"""Actor management router (stub — to be implemented in Task 1.17)."""

# TODO(core): Implement:
# - GET  /actors/           list with filters
# - GET  /actors/{id}       actor detail + content
# - POST /actors/merge      manual merge
# - POST /actors/split      manual split
